# Required Environment Variables for Kavak Trade-Up Engine
# Create a .env file in the root directory with these variables:

REDSHIFT_HOST=your-redshift-cluster.region.redshift.amazonaws.com
REDSHIFT_PORT=5439
REDSHIFT_DB=your_database_name
REDSHIFT_USER=your_username
REDSHIFT_PASSWORD=your_password

# Optional: Logging configuration
LOG_LEVEL=INFO

# Instructions:
# 1. Copy this file to .env in the root directory
# 2. Replace the placeholder values with your actual Redshift credentials
# 3. Make sure the .env file is in your .gitignore to avoid committing credentials 