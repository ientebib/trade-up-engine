fastapi==0.115.13
uvicorn==0.34.3
jinja2==3.1.6
pandas==2.3.0
numpy==2.3.1
numpy-financial==1.0.0
scipy==1.15.3
pydantic==2.11.7
python-multipart==0.0.20
aiofiles==24.1.0
requests==2.32.4 

# Database and Redshift connection
redshift-connector==2.1.7
sqlalchemy==2.0.32  # Keep for other potential uses (ORM, connection pooling, query building), but not for Redshift connection. Redshift uses redshift-connector for native connectivity.
python-dotenv==1.1.0

# Data processing
openpyxl==3.1.5

# Optional: For enhanced CSV processing
chardet==5.2.0

# Server-Sent Events
sse-starlette>=1.2.0

pyarrow>=15.0.0 
tenacity==8.2.3
